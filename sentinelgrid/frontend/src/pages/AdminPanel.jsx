import React from 'react';

const AdminPanel = () => (
  <div>
    <h1>Admin Panel</h1>
    <p>Manage users, events, and system settings (admin only).</p>
  </div>
);

export default AdminPanel;